import 'package:flutter/material.dart';

class Acer extends StatelessWidget {
  const Acer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Acer',
        style: TextStyle(
            fontSize: 30,
            color: Colors.black
        ),),
    );
  }
}
